using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Teste7 : MonoBehaviour {
    // Start is called before the first frame update
    void Start() {
        for (int i = 1; i < 11; i++)
        {
            Debug.Log("algo");
            Debug.Log("O valor do i é: " + i);
        }
    }

    // Update is called once per frame
    void Update() {

    }
}
